// Obsoleto: el estado de incidencias se migró al store de Pinia.
// Se mantiene este alias por compatibilidad; usa `useIncidentsStore` directamente.
export { useIncidentsStore as useIncidents } from '../stores/incidents'
